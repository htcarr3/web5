# frozen_string_literal: true

RSpec.describe Web5 do
  it "has a version number" do
    expect(Web5::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
